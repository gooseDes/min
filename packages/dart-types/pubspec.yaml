name: min_types
description: types generated from zod schemas and some utilities to work
version: 1.0.0
resolution: workspace

environment:
  sdk: ^3.13.0

# Add regular dependencies here.
# dependencies:
# path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  dart_config:
    path: ../../packages/dart-config
