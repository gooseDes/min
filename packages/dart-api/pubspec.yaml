name: min_api
description: Dart api client
publish_to: "none"
version: 1.0.0
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  freezed_annotation: ^3.1.0
  http: ^1.6.0
  socket_io_client: ^3.1.6
  min_types:
    path: ../../packages/dart-types
  logger: ^2.7.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  dart_config:
    path: ../../packages/dart-config
  freezed: ^3.2.3
